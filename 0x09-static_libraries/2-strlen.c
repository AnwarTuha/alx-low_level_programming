#include <stdio.h>
#include <string.h>
#include "main.h"

/**
* _strlen - swaps the values of two integers
*
* @s: pointer to string to be counted
*
* Return: length of given string as int
*/

int _strlen(char *s)
{
	return (strlen(s));
}
