#include "main.h"
#include <stdlib.h>

/**
 * _abs - prints absolute value of input
 *
 * @n: the integer value it receives
 *
 * Return: integer, absolute value of input
 */
int _abs(int n)
{
	return (abs(n));
}
